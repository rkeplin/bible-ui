# Local dev API URLs - copy to .env and adjust as needed.
# For k8s builds, bin/push.sh injects these at build time via .env.production.local.

REACT_APP_API_URL=http://localhost:8084/v1
REACT_APP_USER_API_URL=http://localhost:8083/v1
